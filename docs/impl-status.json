{
  "complete": false,
  "total_items": 26,
  "done_items": 6,
  "current_chunk": "7. detect/base.py + detect/scripted.py: the detector protocol and the shipped test detector",
  "plan_file": "docs/IMPL-PLAN.md",
  "spec_of_record": "docs/DESIGN.md",
  "branch": "feat/safari-classifier",
  "phases": {
    "A_end_to_end_slice": "chunks 1-11",
    "B_formats_states_faillLoud": "chunks 12-13",
    "C_real_detector": "chunk 14",
    "D_training_species_head": "chunks 15-17",
    "E_birds": "chunks 18-19",
    "F_gui": "chunks 20-22",
    "G_loop_closers_polish": "chunks 23-26"
  },
  "notes": "complete=true only when every one of the 26 checklist items in docs/IMPL-PLAN.md is checked AND `uv run --frozen pytest tests/e2e -q` is green over E1-E26 with real captured output. Update done_items and current_chunk after each chunk. As of chunk 6 the suite is still green (7 passed, exit 0 — the CLI-surface tests) and images.py exists: open_source() is the only source reader ('rb'), decode() establishes the single EXIF-transposed frame (I9), sha256 streams raw bytes in 1 MiB chunks, measure_blur() downscales only (ref_edge persisted), and crop() marks a sub-2px box degenerate while still returning it so dominance_ratio stays the only size gate. Verified by scripts/probe_images.py: 48 checks, 0 failures, including 28 real COCO val2017 photographs and all 7 of its non-crowd sub-2px animal boxes. Chunk 7 (detect/base.py + detect/scripted.py) is next; chunk 9 is where scan.py and images.py first get executable e2e coverage (E2/E5/E6/E26), chunk 12's E11/E16 cover blur and every skip reason, and chunk 20's E18 pins the orientation-6 leg end to end."
}
