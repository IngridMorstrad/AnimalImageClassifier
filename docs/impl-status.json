{
  "complete": false,
  "total_items": 26,
  "done_items": 5,
  "current_chunk": "6. images.py: decode, the one coordinate frame, sha256, blur, crop",
  "plan_file": "docs/IMPL-PLAN.md",
  "spec_of_record": "docs/DESIGN.md",
  "branch": "feat/safari-classifier",
  "phases": {
    "A_end_to_end_slice": "chunks 1-11",
    "B_formats_states_faillLoud": "chunks 12-13",
    "C_real_detector": "chunk 14",
    "D_training_species_head": "chunks 15-17",
    "E_birds": "chunks 18-19",
    "F_gui": "chunks 20-22",
    "G_loop_closers_polish": "chunks 23-26"
  },
  "notes": "complete=true only when every one of the 26 checklist items in docs/IMPL-PLAN.md is checked AND `uv run --frozen pytest tests/e2e -q` is green over E1-E26 with real captured output. Update done_items and current_chunk after each chunk. As of chunk 5 the suite is still green (7 passed, exit 0 — the CLI-surface tests) and scan.py exists: all 11 scan-time skip reasons plus §5.2's two decode-time ones, partitioned benign/abnormal at import time so exit_code_for() cannot classify a new reason by accident. FOLLOWUPS F1 is closed ahead of the scanner: ensure_image now refreshes only caller-named columns, with status/run_id behind refresh_state=True, so a re-walk cannot demote a done row to planned. Chunk 6 (images.py) owns decode, the EXIF-transposed frame, sha256, blur and crop; chunk 9 is where scan.py first gets executable e2e coverage and chunk 12's E16 is where every skip reason does."
}
